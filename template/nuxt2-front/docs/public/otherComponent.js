/*
 1. 先import组件；
 2. 再在install函数中注册组件
 */

// 示例：
// import message from '@/views/finance/component/message';

// 注册组件
export default {
    install(Vue) {
        // Vue.component('message', message);
    }
};
