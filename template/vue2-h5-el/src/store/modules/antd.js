const state = {
    formModel: {
        two: {
            labelCol: { span: 8 },
            wrapperCol: { span: 16 },
            wrapperColOffset: { span: 16, offset: 8 }
        },
        five: {
            labelCol: { span: 4 },
            wrapperCol: { span: 20 },
            wrapperColOffset: { span: 20, offset: 4 }
        }
    },
    modelSize: 'small' // large 和 small   输入框高度为 40px 和 24px ，默认高度为 32px
};

const getters = {
};

const mutations = {
};

const actions = {};

export default {
    state,
    getters,
    actions,
    mutations
};
